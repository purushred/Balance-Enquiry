<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="150dp"
    android:layout_height="90dp"
    android:layout_margin="5dp"
    android:background="?attr/selectableItemBackgroundBorderless">

    <RelativeLayout
        android:id="@+id/relativeLayout"
        android:layout_width="145dp"
        android:layout_height="85dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:focusable="true"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/bankLogoView"
            android:layout_width="80dp"
            android:layout_height="50dp"
            android:layout_centerHorizontal="true"
            android:layout_marginRight="3dp"
            android:layout_marginTop="3dp"
            android:contentDescription="Bank Logo"
            android:scaleType="fitCenter"
            android:src="@drawable/dcb_bank" />

        <TextView
            android:id="@+id/bankNameView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/bankLogoView"
            android:layout_centerHorizontal="true"
            android:text="Test Bank" />

        <ImageView
            android:id="@+id/bankFavIcon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:layout_alignTop="@+id/bankLogoView"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            android:padding="5dp"
            android:src="@drawable/ic_favorite_black_24dp" />

    </RelativeLayout>
</android.support.v7.widget.CardView>